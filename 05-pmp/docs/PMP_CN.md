# 05 · PMP 保护区

> 前置：04 特权级切换。本课只做一件事：**设保护 → M/S/U 读写 → 取消 → 再测。**

## 怎么走

1. 启动已是 **PMP: on**（也可在 M 按 `e`）
2. `[M]` 按 `1`/`2`：读/写保护区，应成功  
3. `s` 进 S，再 `1`/`2`：应 trap（load `mcause=5`，store `7`），仍留在 S  
4. `u` 进 U，同样 `1`/`2`：应 trap  
5. 回 M，按 `c` 取消保护  
6. 再进 S/U，`1`/`2`：应成功  

`e`/`c` 只能在 M。S/U 改表会在关匹配时取指 fault。

## 保护区

一块 4K：链接脚本 `.pmp_secret`（SRAM 上一般是 `0x80220000`）。

| 状态 | 表 | S/U 碰这块 |
|------|----|------------|
| **on** | pmp0 拒绝该 4K；pmp1 SRAM RWX；pmp2 UART RW | trap |
| **off** | 全空间放行（与 04 相同） | 成功 |

**off 不是清空 PMP。** C908 实现了 PMP 却没有任何授权时，S/U 默认不能访问，代码和串口会挂。取消保护 = 去掉 secret 那条拒绝。

未加锁时 **M 不看 PMP**，所以 on 时 M 读写仍成功。

## 期望

| 模式 | PMP on | PMP off |
|------|--------|---------|
| M | 读写 OK | 读写 OK |
| S / U | 读 trap 5 / 写 trap 7 | 读写 OK |

异常在 M 处理，`mret` 回原模式。

## 构建

```bash
cd 05-pmp && make
```

返回：[本目录 README](../README.md) · [文档索引](../../docs/README.md)
